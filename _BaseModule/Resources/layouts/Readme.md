# Layout Folder

Important Notes:
- Layouts can be sorted here in extra Folders
- **Please be aware** Layout that uses images, the imageMesh atlas needs to be the Release one(Look Atlas folder readme) that means if your done your layouts locally you need to change that. Example "Arrows" -> "[ProjectName]:Arrows"